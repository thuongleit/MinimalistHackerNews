name: hacker_news
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  firebase: ^5.0.0
  timeago: ^2.0.25

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

